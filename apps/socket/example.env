# Copy to .env.local in this directory (gitignored).
# Monorepo CI publishes: ghcr.io/<github-owner>/tdarts-socket:socket-latest (default branch)
SOCKET_JWT_SECRET=replace-with-same-secret-as-web-auth
ALLOWED_ORIGIN=http://localhost:3000
SOCKET_PORT=8080
# ENABLE_MONITORING=true
