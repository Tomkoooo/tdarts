import { AdminRouteStub } from '@/features/admin/lib/AdminRouteStub';

export default function AdminDashboardPage() {
  return (
    <main className="p-6">
      <AdminRouteStub title="Admin dashboard" note="Build the new interactive dashboard here." />
    </main>
  );
}
