{
  "name": "tdarts-monorepo",
  "private": true,
  "license": "CC BY-NC-SA 4.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "build:web": "turbo build --filter=web",
    "dev:web": "turbo dev --filter=web"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "pnpm@10.18.1",
  "pnpm": {
    "overrides": {
      "@types/react": "19.2.14",
      "@types/react-dom": "19.2.3"
    }
  }
}
